#include <stdio.h>
#include <math.h>

long long minlength(long long x,long long y) {
    
}

int main() {
    int q;
    long long x, y;
    scanf("%d", &q);
    while (q--) {
        scanf("%d %d", &x, &y);
    }
}